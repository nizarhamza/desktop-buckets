<Window x:Class="DesktopBuckets.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Desktop Buckets"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStyle="SingleBorderWindow"
        FontFamily="Segoe UI Variable Text, Segoe UI"
        TextOptions.TextFormattingMode="Ideal"
        UseLayoutRounding="True"
        Background="{DynamicResource App.WindowBackground}">
    <Grid Margin="20" Width="330">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="PromptText"
                   Grid.Row="0"
                   Foreground="{DynamicResource App.TextPrimary}"
                   FontSize="13"
                   Margin="0,0,0,10"
                   Text="Name" />

        <TextBox x:Name="Input"
                 Grid.Row="1"
                 Style="{StaticResource App.TextBox}" />

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,18,0,0">
            <Button x:Name="OkButton"
                    Content="Create"
                    Style="{StaticResource App.AccentButton}"
                    MinWidth="88"
                    IsDefault="True"
                    Click="Ok_Click" />
            <Button Content="Cancel"
                    Style="{StaticResource App.Button}"
                    MinWidth="88"
                    Margin="8,0,0,0"
                    IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
